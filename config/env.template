# Scope3 Campaign API - Environment Configuration Template
# Copy this file to .env and update with your actual values

# ========================================
# Authentication (Required)
# ========================================

# Scope3 API Key (required for all operations)
SCOPE3_API_KEY=your-api-key-here

# Customer ID (required for testing)
CUSTOMER_ID=12345

# ========================================
# Google Cloud Storage Configuration
# ========================================

# GCS Project ID (required for asset uploads)
GCS_PROJECT_ID=your-project-id

# GCS Bucket Name for storing creative assets
GCS_BUCKET_NAME=scope3-creative-assets

# Path to service account credentials JSON file (LOCAL DEVELOPMENT ONLY)
# For production Cloud Run, use service account attachment - DO NOT set this variable
GOOGLE_APPLICATION_CREDENTIALS=./secrets/gcs-credentials.json

# Optional: Custom GCS endpoint (for testing or private cloud)
# GCS_ENDPOINT=https://storage.googleapis.com

# ========================================
# MCP Server Configuration
# ========================================

# Port for MCP server (default: 3000)
# MCP_PORT=3000

# Enable debug logging (default: false)
# DEBUG=true

# ========================================
# Testing Configuration
# ========================================

# Test buyer agent ID (for upload tests)
TEST_BUYER_AGENT_ID=agent-001

# Test asset type (for validation tests)
TEST_ASSET_TYPE=image

# ========================================
# BigQuery Configuration (Optional)
# ========================================

# BigQuery dataset for enhanced features
# BIGQUERY_DATASET=agenticapi

# BigQuery project (defaults to GCS_PROJECT_ID)
# BIGQUERY_PROJECT_ID=your-bigquery-project

# ========================================
# Cache Configuration (Optional)
# ========================================

# Cache TTL for brand agents (milliseconds, default: 300000)
# CACHE_TTL_BRAND_AGENTS=300000

# Cache TTL for campaigns (milliseconds, default: 120000)
# CACHE_TTL_CAMPAIGNS=120000

# Cache TTL for creatives (milliseconds, default: 300000)
# CACHE_TTL_CREATIVES=300000

# Default cache TTL (milliseconds, default: 60000)
# CACHE_TTL_DEFAULT=60000

# ========================================
# Analytics & Monitoring Configuration
# ========================================

# PostHog API Key for user analytics (optional)
# POSTHOG_API_KEY=your-posthog-api-key

# PostHog Host (default: https://app.posthog.com)
# POSTHOG_HOST=https://app.posthog.com

# Log level (debug, info, warn, error - default: info in production, debug in development)
# LOG_LEVEL=info

# ========================================
# Security Configuration (Optional)
# ========================================

# Enable HTTPS for MCP server (default: false)
# HTTPS_ENABLED=false

# HTTPS certificate path (if HTTPS_ENABLED=true)
# HTTPS_CERT_PATH=./cert.pem

# HTTPS private key path (if HTTPS_ENABLED=true)
# HTTPS_KEY_PATH=./key.pem